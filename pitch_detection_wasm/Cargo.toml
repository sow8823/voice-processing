[package]
name = "pitch_detection"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
pitch-detection-fixed = { path = "../pitch_detection_fixed" }
web-sys = { version = "0.3", features = ["console"] }
